/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package extraproject;

/**
 *
 * @author Nurtas
 */
public class LongFormBSTset {
     /* add
            TreeNode<T> res = addHelper(value, root);
            if(res == null)return;
            if(value.compareTo(res.getValue()) < 0)res.setLeft(nd);
            else res.setRight(nd);
            */
    /*
    private TreeNode<T> addHelper(T value, TreeNode<T> cur){
        if(value.compareTo(cur.getValue()) == 0)return null;
        else if(value.compareTo(cur.getValue()) < 0){
            if(cur.getLeft() == null)return cur;
            return addHelper(value, cur.getLeft());
        }
        else{
            if(cur.getRight() == null)return cur;
            return addHelper(value, cur.getRight());
        }
    }
    */
    /* contains
        if(addHelper(value, root) == null)return true;
        else return false;
        */
    
}
